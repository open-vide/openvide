{
  "name": "@openvide/daemon",
  "version": "0.2.3",
  "description": "Persistent session manager for AI CLI tools (Claude Code, Codex, Gemini)",
  "type": "module",
  "bin": {
    "openvide-daemon": "./bin/openvide-daemon.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin/",
    "dist/",
    "LICENSE"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
